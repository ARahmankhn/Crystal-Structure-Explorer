# Crystal Structure Explorer

Crystal Structure Explorer is a Python Streamlit web application scaffold for loading and inspecting crystal structure data from CIF files.

## Setup

1. Create and activate a virtual environment (recommended).
2. Install dependencies:

```bash
pip install -r requirements.txt
```

## Run the app

```bash
streamlit run app.py
```
